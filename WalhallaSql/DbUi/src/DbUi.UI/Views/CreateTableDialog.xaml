<Window x:Class="DbUi.UI.Views.CreateTableDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DbUi.UI.ViewModels.Dialogs"
        xmlns:conv="clr-namespace:DbUi.UI.Converters"
        Title="New Table"
        Width="680" Height="520"
        MinWidth="560" MinHeight="400"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{StaticResource BgPanelBrush}">

    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Table name -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Content="Table name:" VerticalAlignment="Center" />
            <TextBox Grid.Column="1"
                    Text="{Binding TableName, UpdateSourceTrigger=PropertyChanged}"
                    VerticalContentAlignment="Center" />
        </Grid>

        <!-- Toolbar -->
        <ToolBarTray Grid.Row="1" Margin="0,0,0,4">
            <ToolBar>
                <Button Content="Add Column" Command="{Binding AddColumnCommand}" />
                <Button Content="Remove Column" Command="{Binding RemoveColumnCommand}"
                         CommandParameter="{Binding SelectedItem, ElementName=ColumnsGrid}" />
            </ToolBar>
        </ToolBarTray>

        <!-- Columns grid -->
        <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
               Background="{StaticResource BgMainBrush}">
            <DataGrid x:Name="ColumnsGrid"
                      ItemsSource="{Binding Columns}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserResizeColumns="True"
                      CanUserSortColumns="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      GridLinesVisibility="All"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Column Name"
                                         Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                         Width="140" />
                    <DataGridTemplateColumn Header="Data Type" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding DataContext.DataTypes, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                         SelectedItem="{Binding DataType, UpdateSourceTrigger=PropertyChanged}"
                                         BorderThickness="0"
                                         Background="Transparent" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridCheckBoxColumn Header="Allow Nulls"
                                            Binding="{Binding IsNullable}"
                                            Width="90" />
                    <DataGridCheckBoxColumn Header="PK"
                                            Binding="{Binding IsPrimaryKey}"
                                            Width="50" />
                    <DataGridCheckBoxColumn Header="Unique"
                                            Binding="{Binding IsUnique}"
                                            Width="70" />
                    <DataGridTextColumn Header="Max Length"
                                        Binding="{Binding MaxLength, TargetNullValue=''}"
                                        Width="90" />
                    <DataGridTextColumn Header="Precision"
                                        Binding="{Binding Precision, TargetNullValue=''}"
                                        Width="80" />
                    <DataGridTextColumn Header="Scale"
                                        Binding="{Binding Scale, TargetNullValue=''}"
                                        Width="60" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Error -->
        <TextBlock Grid.Row="3"
                  Text="{Binding ErrorMessage}"
                  Foreground="{StaticResource ErrorBrush}"
                  Visibility="{Binding ErrorMessage, Converter={StaticResource BoolToVis}}"
                  TextWrapping="Wrap"
                  Margin="0,8,0,0" />

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="OK"
                    Command="{Binding OkCommand}"
                    IsDefault="True"
                    MinWidth="80" Margin="0,0,8,0" />
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    IsCancel="True"
                    MinWidth="80" />
        </StackPanel>
    </Grid>
</Window>
